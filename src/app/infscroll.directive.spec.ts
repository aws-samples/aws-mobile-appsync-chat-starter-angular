import { InfscrollDirective } from './infscroll.directive';

describe('InfscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new InfscrollDirective();
    expect(directive).toBeTruthy();
  });
});
