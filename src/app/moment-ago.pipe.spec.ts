import { MomentAgoPipe } from './moment-ago.pipe';

describe('MomentAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new MomentAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
